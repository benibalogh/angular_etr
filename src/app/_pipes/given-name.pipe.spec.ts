import { GivenNamePipe } from './given-name.pipe';

describe('GivenNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GivenNamePipe();
    expect(pipe).toBeTruthy();
  });
});
